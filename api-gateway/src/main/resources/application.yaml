spring:
  application:
    name: api-gateway

server:
  port: ${PORT:9091}

jwt:
  secret: ${JWT_SECRET:your-super-secret-key-minimum-32-characters-long}

services:
  auth-service: ${SERVICES_AUTH_SERVICE:http://localhost:9093}
  student-service: ${SERVICES_STUDENT_SERVICE:http://localhost:9095}
  attendance-service: ${SERVICES_ATTENDANCE_SERVICE:http://localhost:9092}
  notification-service: ${SERVICES_NOTIFICATION_SERVICE:http://localhost:9094}

sentry:
  dsn: ${SENTRY_DSN:}
  traces-sample-rate: 1.0
  enabled: ${SENTRY_ENABLED:false}
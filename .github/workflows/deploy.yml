name: Build and Deploy

on:
  push:
    branches: [ main ]

jobs:
  # ─────────────────────────────────────────────
  # Job 1 — Build Docker images and push to GHCR
  # ─────────────────────────────────────────────
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up JDK 21
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push api-gateway
        uses: docker/build-push-action@0a97817b6ade9f46837855d676c4cca3a2471fc9 # v4
        with:
          context: ./api-gateway
          push: true
          tags: ghcr.io/ajynky/api-gateway:latest

      - name: Build and push auth-service
        uses: docker/build-push-action@0a97817b6ade9f46837855d676c4cca3a2471fc9 # v4
        with:
          context: ./auth-service
          push: true
          tags: ghcr.io/ajynky/auth-service:latest

      - name: Build and push student-service
        uses: docker/build-push-action@0a97817b6ade9f46837855d676c4cca3a2471fc9 # v4
        with:
          context: ./student-service
          push: true
          tags: ghcr.io/ajynky/student-service:latest

      - name: Build and push attendance-service
        uses: docker/build-push-action@0a97817b6ade9f46837855d676c4cca3a2471fc9 # v4
        with:
          context: ./attendance-service
          push: true
          tags: ghcr.io/ajynky/attendance-service:latest

      - name: Build and push notification-service
        uses: docker/build-push-action@0a97817b6ade9f46837855d676c4cca3a2471fc9 # v4
        with:
          context: ./notification-service
          push: true
          tags: ghcr.io/ajynky/notification-service:latest

  # ─────────────────────────────────────────────
  # Job 2 — Sync GitHub Secrets → Railway env vars
  # Runs only after images are successfully built
  # ─────────────────────────────────────────────
  sync-secrets:
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: production
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}

    steps:
      - name: Install Railway CLI
        run: npm install -g @railway/cli

      # ── api-gateway ──────────────────────────
      - name: Sync api-gateway secrets
        run: |
          railway variables --set \
            "JWT_SECRET=${{ secrets.JWT_SECRET }}" \
            "SENTRY_DSN=${{ secrets.SENTRY_DSN_GATEWAY }}" \
            "SENTRY_ENABLED=true" \
            "SERVICES_AUTH_SERVICE=http://auth-service.railway.internal:8080" \
            "SERVICES_STUDENT_SERVICE=http://student-service.railway.internal:8080" \
            "SERVICES_ATTENDANCE_SERVICE=http://attendance-service.railway.internal:8080" \
            "SERVICES_NOTIFICATION_SERVICE=http://notification-service.railway.internal:8080" \
            --service api-gateway \
            --environment production

      # ── auth-service ─────────────────────────
      - name: Sync auth-service secrets
        run: |
          railway variables --set \
            "SPRING_DATASOURCE_URL=${{ secrets.SPRING_DATASOURCE_URL }}" \
            "SPRING_DATASOURCE_USERNAME=${{ secrets.SPRING_DATASOURCE_USERNAME }}" \
            "SPRING_DATASOURCE_PASSWORD=${{ secrets.SPRING_DATASOURCE_PASSWORD }}" \
            "JWT_SECRET=${{ secrets.JWT_SECRET }}" \
            "JWT_EXPIRATION=${{ secrets.JWT_EXPIRATION }}" \
            "SENTRY_DSN=${{ secrets.SENTRY_DSN_AUTH }}" \
            "SENTRY_ENABLED=true" \
            --service auth-service \
            --environment production

      # ── student-service ──────────────────────
      - name: Sync student-service secrets
        run: |
          railway variables --set \
            "SPRING_DATASOURCE_URL=${{ secrets.SPRING_DATASOURCE_URL }}" \
            "SPRING_DATASOURCE_USERNAME=${{ secrets.SPRING_DATASOURCE_USERNAME }}" \
            "SPRING_DATASOURCE_PASSWORD=${{ secrets.SPRING_DATASOURCE_PASSWORD }}" \
            "JWT_SECRET=${{ secrets.JWT_SECRET }}" \
            "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" \
            "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            "SERVICES_NOTIFICATION_SERVICE=http://notification-service.railway.internal:8080" \
            "SENTRY_DSN=${{ secrets.SENTRY_DSN_STUDENT }}" \
            "SENTRY_ENABLED=true" \
            --service student-service \
            --environment production

      # ── attendance-service ───────────────────
      - name: Sync attendance-service secrets
        run: |
          railway variables --set \
            "SPRING_DATASOURCE_URL=${{ secrets.SPRING_DATASOURCE_URL }}" \
            "SPRING_DATASOURCE_USERNAME=${{ secrets.SPRING_DATASOURCE_USERNAME }}" \
            "SPRING_DATASOURCE_PASSWORD=${{ secrets.SPRING_DATASOURCE_PASSWORD }}" \
            "JWT_SECRET=${{ secrets.JWT_SECRET }}" \
            "SERVICES_NOTIFICATION_SERVICE=http://notification-service.railway.internal:8080" \
            "SENTRY_DSN=${{ secrets.SENTRY_DSN_ATTENDANCE }}" \
            "SENTRY_ENABLED=true" \
            --service attendance-service \
            --environment production

      # ── notification-service ─────────────────
      - name: Sync notification-service secrets
        run: |
          railway variables --set \
            "SPRING_DATASOURCE_URL=${{ secrets.SPRING_DATASOURCE_URL }}" \
            "SPRING_DATASOURCE_USERNAME=${{ secrets.SPRING_DATASOURCE_USERNAME }}" \
            "SPRING_DATASOURCE_PASSWORD=${{ secrets.SPRING_DATASOURCE_PASSWORD }}" \
            "RESEND_API_KEY=${{ secrets.RESEND_API_KEY }}" \
            "RESEND_FROM_EMAIL=${{ secrets.RESEND_FROM_EMAIL }}" \
            "TWILIO_ACCOUNT_SID=${{ secrets.TWILIO_ACCOUNT_SID }}" \
            "TWILIO_AUTH_TOKEN=${{ secrets.TWILIO_AUTH_TOKEN }}" \
            "TWILIO_FROM_NUMBER=${{ secrets.TWILIO_FROM_NUMBER }}" \
            "SENTRY_DSN=${{ secrets.SENTRY_DSN_NOTIFICATION }}" \
            "SENTRY_ENABLED=true" \
            --service notification-service \
            --environment production
